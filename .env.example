# ============================================================
# EDIP — Docker environment variables
# Copy this file to .env and fill in your values.
# NEVER commit .env — it contains real credentials.
# ============================================================

POSTGRES_USER=edip_user
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_DB=edip_db
POSTGRES_PORT=5432
